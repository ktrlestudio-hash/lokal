/**
 * Motion Wrappers para LOKAL
 * Wrappers reutilizables de Framer Motion (motion)
 * que respetan los design tokens, dark mode y responsive del proyecto.
 */

export { motion, AnimatePresence, useInView, useMotionValue, useTransform } from 'motion/react';

// Re-exportar todo de motion/react para conveniencia
export * from 'motion/react';
